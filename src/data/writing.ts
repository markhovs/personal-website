export interface WritingItem {
  title: string;
  url: string;
  date: string;
  description: string;
}

// External articles and links. Internal blog posts live in content/writing/*.md.
const data: WritingItem[] = [];

export default data;
